[target.xtensa-esp32-none-elf]
runner = "espflash flash --monitor --chip esp32 --baud 230400 --log-format defmt"

[env]
DEFMT_LOG="info"
# DEFMT_LOG="info,esp_wmata_pids=debug"

[build]
rustflags = [
  "-C", "link-arg=-nostartfiles",
]

target = "xtensa-esp32-none-elf"

[unstable]
build-std = ["alloc", "core"]
